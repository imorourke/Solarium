#include <cbapp.cb>

global HELLO_VAL: *u8 = "Hello, world!\n";

fn main(argv: **u8, argc: u32) i32 {
    def data: *u8 = malloc(100);
    memset(data, 0, 100);
    def len: u32 = 30;
    memcpy(data, HELLO_VAL, len + 1);
    k_io_print(data);
    free(data);
    return 0;
}
